%h1
  Shoots index

- @shoots.each do |shoot|
  .shoot{:id => dom_id(shoot)}
    %h3
      = shoot.artist
    %p
      = shoot.description
    %h5
      = shoot.location
    = link_to 'edit', edit_admin_shoot_path(shoot)
    = link_to 'show', shoot_path(shoot)
    = link_to 'Destroy', admin_shoot_path(shoot), method: :delete
    - if shoot.video_processing
      Processing
    - elsif shoot.is_published
      Published
    - else
      Draft

= link_to 'Add new shoot', new_admin_shoot_path